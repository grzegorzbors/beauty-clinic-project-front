import { makeStyles } from "@material-ui/core/styles";

const footer = makeStyles((theme) => ({
  footer: {
    minHeight: "5vh",
    position: "fixed",
    bottom: 0,
    left: 0,
  },
}));

export default footer;
