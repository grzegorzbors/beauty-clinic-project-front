import { makeStyles } from "@material-ui/core/styles";

const form = makeStyles((theme) => ({
  newAppointmentForm: {
    display: "flex",
    flexDirection: "column",
    // maxWidth: "40%",
  },
}));

export default form;
