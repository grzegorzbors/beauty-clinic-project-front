import { makeStyles } from "@material-ui/core/styles";

const privacyPolicy = makeStyles(() => ({
  list: {
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
  },
  cardDesktop: {
    width: "70%",
  },
}));

export default privacyPolicy;
